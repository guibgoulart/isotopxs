{
  "name": "isotopxs-site",
  "private": true,
  "description": "Site estático da ISOTOPXS. Sem build step no front-end — este package.json existe só para as Netlify Functions (netlify/functions) terem suas dependências instaláveis e para rodar 'netlify dev' localmente.",
  "scripts": {
    "dev": "netlify dev",
    "test": "node --test"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "posthog-node": "^5.51.2"
  }
}
